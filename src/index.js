import React from 'react';
import ReactDOM from 'react-dom';

const element = (
  <div>
    <h1>Hola, soy Joaquin</h1>
    <p>Soy Técnico de DAM</p>
  </div>
);

const container = document.getElementById('app');

ReactDOM.render(element,container);
